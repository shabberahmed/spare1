export enum TaskType {
  LabReports = 'labreports',
  Pickup = 'pickup',
  Hotels = 'hotels',
}
export enum TaskStatus {
  Pending = 'Pending',
  InProgress = 'InProgress',
  Completed = 'Completed',
  Canceled = 'Canceled',
}
